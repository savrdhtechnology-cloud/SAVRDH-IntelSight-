# Client-side Supabase configuration (safe anon key only)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=

# Server-side public intelligence connectors.
# Configure these only in Vercel Environment Variables. Never prefix secret keys with VITE_.
TAVILY_API_KEY=

# Optional fallback web-search provider.
BRAVE_SEARCH_API_KEY=

# Optional future defensive exposure connector.
HIBP_API_KEY=
